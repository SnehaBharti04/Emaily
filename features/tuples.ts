const pepsi = { color: "red", carbonated: false, sugar: 50 };

// type alias

type drink = [string, boolean, number]


const newDrink: drink = ["brown", true, 40]

const tea : drink = ["black", false, 10]

const specs : [number, number] = [3222, 4555]

const power ={
    weight : 4555,
    age: 34
}